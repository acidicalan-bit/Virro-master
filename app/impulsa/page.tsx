import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { Button } from "@/components/ui/button";
export const metadata: Metadata = { title: "Cómo funciona", description: "El método VIRRO IMPULSA: diagnosticar, visualizar, priorizar, implementar, adoptar y medir." };
const steps = [["Diagnosticar","Observamos imagen, presencia, atención, conversión, operación y adopción con evidencia."],["Visualizar","Construimos mockups y prototipos aplicados a tu giro antes de producir."],["Priorizar","Clasificamos ahora, después y todavía no según presupuesto y capacidad."],["Implementar","Coordinamos Studio, Systems y Academy con responsables y aceptación."],["Adoptar","Capacitamos por rol y documentamos para que la herramienta se use."],["Medir","Comparamos una línea base con cambios observables y sus limitaciones."]];
export default function Page() { return <main id="main-content"><section className="page-hero section"><div className="shell"><span className="eyebrow">VIRRO IMPULSA</span><h1 className="display">Un programa para construir el siguiente paso, no para venderte todo.</h1><p className="lead">Diagnosticamos, demostramos e implementamos una ruta progresiva que tu negocio sí puede sostener.</p><div className="actions"><Button asChild variant="acid" size="lg"><Link href="/diagnostico">Solicitar diagnóstico <ArrowRight /></Link></Button><Button asChild variant="outline" size="lg"><Link href="/planes">Ver modelos de atención</Link></Button></div></div></section><section className="section section-rule"><div className="shell"><span className="eyebrow">El método</span><h2 className="section-title">Claridad antes de complejidad.</h2><div className="route-grid">{steps.map(([title,copy],index) => <article key={title}><span>0{index+1}</span><h3>{title}</h3><p>{copy}</p></article>)}</div></div></section></main>; }
