import type { Metadata } from "next";
import Link from "next/link";
import { cases } from "@/content/site";
export const metadata: Metadata = { title: "Transformaciones", description: "Casos conceptuales para explorar cambios físicos, visuales, digitales y operativos." };
export default function Page() { return <main id="main-content"><section className="page-hero section"><div className="shell"><span className="eyebrow">Transformaciones</span><h1 className="display">Antes de construir, hacemos visible lo posible.</h1><p className="lead">Cada proyecto indica su tipo de evidencia. Los siguientes casos son conceptuales: demuestran capacidades, no resultados obtenidos.</p></div></section><section className="section section-rule"><div className="shell"><div className="case-grid">{cases.map((item) => <Link className="case-card" href={`/transformaciones/${item.slug}`} key={item.slug} style={{ "--case": item.color } as React.CSSProperties}><div className="case-visual"><span className="case-label">{item.label}</span></div><div className="case-body"><small>{item.sector}</small><h2>{item.name}</h2><p>{item.before} → {item.after}</p></div></Link>)}</div></div></section></main>; }
