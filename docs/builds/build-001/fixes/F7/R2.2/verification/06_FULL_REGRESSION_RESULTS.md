# Full Regression Results

Independent full Vitest: `47` files passed, `5` skipped; `434` tests passed, `11` skipped, with `--testTimeout=30000` to avoid load-related timeout noise.

TypeScript: PASS. ESLint: PASS. Assurance manifest check: PASS. Production build: PASS, including TypeScript and 19 static pages. F1 SQL, R1 model and F2 application lanes all passed independently.

The first clone attempt from the repository common directory did not contain the local candidate commit; it was abandoned without changes. Verification continued from a fresh clone of the R2.2 candidate worktree at the exact required SHA.
