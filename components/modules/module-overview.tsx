import Link from "next/link";
import { ArrowRight, CheckCircle2, FileOutput, Layers3, Plus } from "lucide-react";
import type { VirroModule } from "@/lib/config/modules";
import { mockEvents } from "@/lib/data/mock-events";
import { StatusPill } from "@/components/ui/status-pill";

export function ModuleOverview({ module }: { module: VirroModule }) {
  const Icon = module.icon;
  const related = mockEvents.filter((event) => event.packType === module.id);
  return <div className="space-y-6"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-[11px] font-semibold uppercase tracking-[.16em] text-teal-300">{module.eyebrow}</p><h1 className="text-2xl font-semibold tracking-[-.035em] md:text-[30px]">{module.label}</h1><p className="mt-2 max-w-2xl text-sm text-[var(--muted)]">{module.description}</p></div><Link href="/inbox" className="inline-flex h-10 items-center justify-center gap-2 rounded-lg bg-teal-300 px-4 text-xs font-semibold text-slate-950"><Plus size={15}/>Analyze new input</Link></section><section className="grid gap-4 lg:grid-cols-[1.2fr_.8fr]"><article className="panel p-5"><div className="flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-teal-400/10 text-teal-300"><Icon size={19}/></div><div><h2 className="text-sm font-semibold">Pack capabilities</h2><p className="text-[11px] text-[var(--subtle)]">Operational outputs available in this module</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-2">{module.capabilities.map((capability)=><div key={capability} className="flex items-center gap-3 rounded-xl border border-[var(--border)] bg-[var(--panel-soft)] p-4"><CheckCircle2 size={15} className="text-teal-300"/><span className="text-xs">{capability}</span></div>)}</div></article><article className="panel p-5"><div className="flex items-center gap-2"><Layers3 size={16} className="text-indigo-300"/><h2 className="text-sm font-semibold">Readiness model</h2></div><p className="mt-3 text-xs leading-5 text-[var(--muted)]">This pack evaluates intent, receiver, dependencies, constraints, missing context and action boundaries before producing an operational output.</p><div className="mt-4 rounded-lg border border-[var(--border)] bg-[var(--panel-soft)] p-3 text-[10px] leading-4 text-[var(--subtle)]">All scores are estimates designed to support judgment—not replace it.</div></article></section><section className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-[var(--border)] px-5 py-4"><div><h2 className="text-sm font-semibold">Active Understanding Events</h2><p className="mt-1 text-[11px] text-[var(--subtle)]">Events currently associated with this analysis context</p></div><FileOutput size={16} className="text-[var(--subtle)]"/></div>{related.length?<div className="divide-y divide-[var(--border)]">{related.map((event)=><div key={event.id} className="flex flex-col gap-3 px-5 py-4 sm:flex-row sm:items-center"><div className="flex-1"><p className="text-xs font-medium">{event.title}</p><p className="mt-1 text-[10px] text-[var(--subtle)]">{event.id} · {event.targetTeam} · {event.targetRole}</p></div><StatusPill status={event.status}/><div className="text-xs font-semibold">{event.scores.virroScore}<span className="font-normal text-[var(--subtle)]"> / 100</span></div><ArrowRight size={14} className="text-[var(--subtle)]"/></div>)}</div>:<div className="px-5 py-12 text-center"><p className="text-sm">No active events in this view</p><p className="mt-1 text-xs text-[var(--subtle)]">Capture an input to create the first operational understanding record.</p></div>}</section></div>;
}
